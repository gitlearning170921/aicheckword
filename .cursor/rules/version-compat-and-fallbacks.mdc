---
description: 版本兼容与回退策略规范（FastAPI/Swagger/依赖差异、参数兼容、外部 CDN 不可达等）。避免“在我机器上能用”与重复返工
alwaysApply: true
---

# 版本兼容与回退策略（项目规范）

本规则用于：在依赖版本不一致、网络受限（CDN 被拦截）、或上游库 API 变更时，仍能稳定运行，并且改动不破坏系统内部接口。

## 1) 新增/修改依赖前的原则

- **先判断“必须新增”还是“可用回退实现”**：能用内置能力/小改实现就不加依赖。
- **依赖版本不写死到精确补丁**（除非必要），但要写清下限与关键不兼容点。
- **不使用“仅某版本才存在”的参数/构造器字段**，除非：
  - 检测到版本满足；或
  - 提供运行时回退分支（try/except 或 capability check）。

## 2) 兼容性实现模式（必须至少选一个）

- **capability check**：检测属性/函数是否存在再调用。
- **try/except fallback**：先走新路径，失败走老路径（记录可读错误）。
- **隔离适配层**：把兼容逻辑放在单独函数/模块（例如 `*_compat.py`），上层只调用统一接口。

## 3) 网络/CDN 不可达的规则（Swagger 等）

- **禁止只绑定单一 CDN**：必须提供至少 2 个备选来源（不同域名/供应商）。
- **必须有“失败提示”**：前端资源加载失败时不能白屏，要给出明确错误与排查方向。
- 如客户网络环境高度受限：优先提供**自托管静态资源**方案（本地 `StaticFiles` 或打包进项目）。

## 4) 外部开放 API 的改动边界

- 对外开放接口的变更必须做到：
  - **不影响内部接口**（仅控制 schema/文档可见性时，确保实际路由仍可调用）。
  - **向后兼容**：已有字段/响应结构尽量保留（例如保留旧顶层键，同时新增 `fields`）。
  - **默认值可用**：新增必填字段要有迁移路径或清晰错误提示。

## 5) 必做验证（改完立刻做）

- **语法检查**：`py_compile`（或等价）。
- **关键路径自测**：最小化请求/调用（例如生成 OpenAPI、访问 `/docs` HTML、访问 `/openapi.json`）。
- **失败信息可读**：异常需要能定位到具体组件（版本/网络/参数）而不是空字符串。

