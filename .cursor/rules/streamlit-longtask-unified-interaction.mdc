---
description: Streamlit 长耗时任务统一交互（进度条、状态文本、失败可恢复、无匹配兜底）约束
globs: src/app.py,src/core/document_draft_generator.py
---

# Streamlit 长任务统一交互（aicheckword）

## 1) 长任务必须可见进度

- 凡触发 LLM 生成、批量导出、批量审核、审核后修改等明显耗时操作，页面必须显示：
  - `st.progress`（百分比）
  - 当前阶段文本（例如「训练输入文件」「生成中 2/8」「导出中」）
- 进度更新优先走后端回调（如 `progress_cb(msg, frac)`），避免仅显示固定 spinner。
- 兼容旧版 Streamlit：`st.progress(..., text=...)` 不可用时，降级为 `st.progress(value)` + `st.caption()`。

## 2) 失败要可恢复，不要“硬阻断”

- 目标文件名未匹配模板案例、未上传同名基础文件等场景，不得直接终止整批流程。
- 必须提供兜底路径（至少其一）：
  - 按目标名归一化（去前缀、basename）进行二次匹配；
  - 部分命中按命中项继续执行；
  - 未命中项降级为非就地生成（文本输出）；
  - base-only 场景用案例模板文本或基础文件文本兜底继续生成。

## 3) 导航与 Session 安全

- 不可在控件（如 `st.radio(key=...)`）已实例化后再写同 key 的 `session_state`。
- 跨页面跳转使用 “pending key” 模式：
  - 触发处写 `_pending_main_function_nav_page`
  - 在 `main()` 中、控件实例化前消费并落到真实 key。

## 4) 多页面一致性要求

- 同类功能页（初稿生成、审核后修改、历史重跑）交互尽量一致：
  - 阶段提示文案风格一致
  - 失败提示包含「原因 + 可继续方案」
  - 结果区支持下载与日志 JSON 查看
- 新增页面若引入长任务，必须复用同类模式，不得回退为“只有按钮 + 无反馈”。
