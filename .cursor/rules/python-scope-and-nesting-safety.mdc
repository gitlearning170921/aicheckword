---
description: 防止 Python 嵌套作用域/层级错误（UnboundLocalError、缩进错位、闭包写外层变量、批量替换导致结构破坏）的项目规范
alwaysApply: true
---

# Python 作用域与嵌套层级安全规范（避免重复踩坑）

本规则用于减少两类常见事故：  
1) 嵌套函数/闭包对外层变量读写导致 `UnboundLocalError`；  
2) 代码块缩进/层级被误改（尤其是“批量替换/脚本加缩进”后），逻辑悄悄跑偏。

## 1) 嵌套函数读写外层变量：强制写法

- **只读外层变量**：允许直接读取，不要在内层对同名变量赋值。
- **需要写外层变量**：必须显式声明：
  - 内层函数修改外层函数变量：`nonlocal var`
  - 内层函数修改模块级变量：`global var`（尽量避免）
- **推荐替代方案（更稳）**：把可变状态放进 dict / dataclass / `st.session_state`，避免 `nonlocal` 链式传播。

### 典型反例（会触发 UnboundLocalError）

- 内层函数里有 `x = ...`，同一函数更早的位置又读取 `x`，Python 会把 `x` 判定为局部变量，从而“引用前未赋值”。

## 2) 控制嵌套层级与职责边界

- 单个 UI handler / tab 函数不应承担“取配置 + 读文件 + 组上下文 + 调用 agent + 写历史 + 清理临时文件”全部职责。
- 超过 2 层嵌套时应拆分为：
  - **纯函数**：输入→输出（不读 session、不做 I/O）
  - **I/O 函数**：只做加载/保存/网络
  - **UI glue**：只做表单与调用

## 3) 避免“字符串替换”造成缩进/层级破坏

- 任何涉及 Python 代码结构的变更：
  - 优先用结构化补丁（清晰的上下文行）而非“模糊匹配替换”。
  - 替换后必须做一次 `py_compile`（或等价语法检查），并目视检查关键缩进块（`with/try/for/def`）。
- 若必须做批量缩进/重排：只允许对**明确行号区间**操作，并且操作后必须再次检查关键块是否仍在预期作用域内。

## 4) 约束：闭包中禁止“同名覆盖”

- 内层函数禁止创建与外层同名的局部变量（除非显式 `nonlocal`/`global` 且有充分理由）。
- 如果确实需要同名：改名为 `_ctx/_state/_local_*`，避免误判作用域。

